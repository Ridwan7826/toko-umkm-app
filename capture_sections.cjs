const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.setViewportSize({ width: 1280, height: 720 }); await page.goto('http://127.0.0.1:8001/', { waitUntil: 'networkidle' }); await page.locator('#kategori').screenshot({ path: 'docs/screenshots/kategori.png' }); await page.locator('#produk-terlaris').screenshot({ path: 'docs/screenshots/produk_terlaris.png' }); await browser.close(); console.log('Sections captured!'); })();
