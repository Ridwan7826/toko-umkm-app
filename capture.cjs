const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.setViewportSize({ width: 1280, height: 720 }); await page.goto('http://127.0.0.1:8000/login', { waitUntil: 'networkidle' }); await page.screenshot({ path: 'docs/screenshots/login.png' }); await browser.close(); console.log('Screenshot updated!'); })();
